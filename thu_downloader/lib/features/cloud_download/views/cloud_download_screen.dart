import 'package:flutter/material.dart';

class CloudDownloadScreen extends StatelessWidget {
  const CloudDownloadScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('云盘下载'),
      ),
      body: const Center(
        child: Text('云盘下载功能开发中...'),
      ),
    );
  }
} 